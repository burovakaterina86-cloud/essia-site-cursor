# PROJECT CONTEXT: AI & AUTOMATION PORTFOLIO (Katerina)
Ты создаешь стильный, быстрый одностраничный лендинг-портфолио для эксперта по AI-ботам и автоматизации (Make.com, Python).
Основной трафик идет из Telegram, поэтому Mobile First — это закон.

# 1. TECH STACK & TOOLS
- **Core:** HTML5, TailwindCSS (подключи через CDN для старта).
- **Scripting:** Vanilla JavaScript (ES6+).
- **Icons:** FontAwesome (для иконок категорий и соцсетей).
- **Font:** 'Inter' или 'Unbounded' (Google Fonts) для техно-вайба.

# 2. DESIGN SYSTEM & VISUALS
Строго следуй этой цветовой схеме и типографике.

## TYPOGRAPHY (Google Fonts)
- **Headings:** 'Unbounded' (вес 600/700) — для заголовков. Это современный, широкий "техно" шрифт.
- **Body:** 'Inter' или 'Plus Jakarta Sans' (вес 400/500) — для основного текста. Максимальная читаемость.
- **Code/Tags:** 'JetBrains Mono' — для тегов (Python, Make, API).

## COLOR PALETTE (Tailwind config)
Используй эти конкретные HEX-коды, не дефолтные цвета Tailwind:

1. **Backgrounds:**
   - Page Bg: `#020617` (Deep Space Blue - почти черный)
   - Card Bg: `rgba(30, 41, 59, 0.4)` (Glassmorphism effect)
   - Border: `#1e293b` (Slate-800)

2. **Accents (Gradients):**
   - **Primary (AI Magic):** Gradient from `#7c3aed` (Violet-600) to `#db2777` (Pink-600).
   - **Secondary (Automation/Money):** Text accents in `#10b981` (Emerald-500).
   - **Hover Glow:** `shadow-[0_0_20px_rgba(124,58,237,0.5)]`

3. **Text:**
   - Headings: `#f8fafc` (Slate-50)
   - Body: `#94a3b8` (Slate-400)

## UI COMPONENTS
- **Buttons:** Градиентный фон, закругление `rounded-xl`, эффект нажатия `active:scale-95`.
- **Cards (Bento):** `backdrop-filter backdrop-blur-lg`, тонкая обводка `border border-white/10`.
## 💎 ПРАВИЛА ПРОДАЮЩЕГО ДИЗАЙНА
- **Заголовки:** Главный заголовок — "AI-решения для бизнеса".
- **Психология цен:** Всегда использовать цифры 7 и 9 (например, 14 900 или 15 700).
- **Интерактив:** - Кнопки: Плавное увеличение + усиление свечения (glow) при наведении.
  - Карточки кейсов: Эффект "Flip Card" (переворот). На лицевой стороне — название и иконка, на обороте — "Какие боли решает" и "Кому подходит".
- **Триггеры:** Вставить блоки "Подпишись" и "Осталось 2 места на аудит" (или аналогичные).
- **SEO:** Все тексты в блоке FAQ должны содержать ключи: "автоматизация бизнеса", "ии-боты для телеграм", "внедрение нейросетей", "разработка mcp серверов".
# 3. CONTENT STRUCTURE
Секции сайта:
1.  **Header:** Фото/Аватар, Имя "Катерина", Заголовок "AI-решения для бизнеса", Кнопка "Связаться" (ведет на бота).
2.  **Filter Bar:** Кнопки для фильтрации кейсов (Все, Боты, Маркетинг, Учет, RAG). Это важно, так как кейсов много.
3.  **Projects Grid:** Сетка с кейсами. Данные брать из `data.js`.
    - У карточки должны быть теги (Make, Python, Telegram).
    - При клике — переход по ссылке (target="_blank").
4.  **Footer:** Копирайт и повторная кнопка связи.

# 4. SPECIAL INSTRUCTIONS FOR DATA
- Не пиши кейсы в HTML вручную! Используй файл `data.js` и рендери их через JavaScript.
- Добавь функционал фильтрации: при клике на категорию "Маркетинг" скрывай остальные карточки с красивой анимацией.

# 5. UX/UI DETAILS
- Ссылки на Telegram должны открываться в новом окне.
- Добавь "Badge" (плашку) на карточки: "Популярное" или "New", если это указано в данных.
- Кнопка "Связаться" должна быть плавающей (Fixed) внизу экрана на мобильных устройствах, чтобы всегда быть под пальцем.